# 贡献指南

## CI 与依赖维护

提交 PR 时，尽量在同一 PR 内解决 CI 暴露的仓库维护问题，避免留下已知失败。

依赖时效性检查属于每次开发的日常维护。如果 CI 发现过期依赖，应将必要的升级纳入当前 PR，即使它与主要功能修改没有直接关系；“与本次改动无关”不是忽略依赖告警的理由。

依赖升级应限于检查指出的范围，并沿用仓库现有的版本策略和依赖管理工具。如果确有兼容性或风险原因无法升级，应在 PR 中说明原因和后续处理方式，而不是跳过或缩小检查范围。
