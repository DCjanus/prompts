# 本机个人约定

这些约定通过软链接安装到 Codex 用户目录，适用于本机各项目；目标项目由当前任务确定，不从本文件或 skill 的安装位置推断。
项目明确约定优先于个人默认，同层级优先采用更贴近目标目录和任务的要求；用户当前明确要求优先于个人默认和 skill 指南。

## 沟通与工作边界

- 默认使用中文，用户为 DCjanus。回复先给结论，简洁说明必要依据。
- 文件和 URL 使用 Markdown 链接。
- 修改项目文件前，读取与目标改动相关的 AGENTS.md、CONTRIBUTING.md、README.md 等项目规范。
- 在已授权范围内完成工作，常规实现细节自行判断；需要澄清时继续不依赖答案的工作，不把局部不确定性扩大成整个任务的停顿。
- 不把代码修改扩展为未授权的提交、推送、平台写入或合并；已有授权无需重复确认。
- 若 skill 要求导致暂停或额外确认，指出具体文件和条款，并区分明确要求与自己的解释。
- 偏好直接、可维护的实现；没有明确需求时不增加历史兼容层，确需额外复杂度时说明成本与替代方案。
- 破坏既有对外契约时，显著标记 breaking change，说明影响与迁移方法。
- 保留用户已有改动；默认不改写 Git 历史，例外按用户或项目明确要求执行。

## 工具与验证

- 沿用项目工具链；项目未指定时，前端优先 pnpm/pnpx，本机临时 Python 使用 uv run --script，依赖保存在脚本中。
- 依赖声明、锁文件和受工具管理的版本字段通过对应工具修改；本次新增或升级依赖优先采用满足项目约束的最新稳定版本，不顺带升级无关依赖。
- 测试优先覆盖对外契约、回归、边界和高风险路径，避免锁定实现细节。
- 为 BUG 新增回归测试时，先确认测试因目标问题失败，再修复并保留测试。
- 完成项目要求及与改动风险匹配的检查；通过后无新证据不重复或扩大验证。修 BUG 未补回归测试或必要验证未完成时，简述原因。

## 按需工作流

仅在任务相关时使用以下 skills，从可用技能目录定位实际 SKILL.md；脚本和 references 相对该 skill 目录解析，目标项目单独指定。

- Git 提交与协作交付：repository-workflow；GitHub/GitLab 交互：对应平台 skill。
- 临时 Python、执行环境与 Python 检查：python-execution；可复用单文件 CLI：uv-cli-creator。
- 依赖或项目版本变更：dependency-management；技术选型：dcjanus-preferences。
