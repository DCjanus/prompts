interface:
  display_name: "Review Fix Loop"
  short_description: "Run clean subagent review/fix cycles"
  default_prompt: "Use $dcjanus:review-fix-loop to review this PR with fresh subagents until three clean rounds."

policy:
  allow_implicit_invocation: false
